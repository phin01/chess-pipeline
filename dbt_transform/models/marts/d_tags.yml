models:
  - name: d_tags
    description: "Dimension table for tag names. Built from unnesting the labeled_tags array in the int_chess_puzzles model"
    columns:
      - name: tag_name
        description: "Name of the tag associated with chess puzzles"
        tests:
          - not_null
          - unique